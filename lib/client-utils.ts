export const handleClientDeleted = async () => {
  // This function is intentionally empty as the real-time listener
  // in ClientsPageLayout automatically updates the UI upon deletion.
  // It serves as a placeholder for the onClientDeleted prop.
}
