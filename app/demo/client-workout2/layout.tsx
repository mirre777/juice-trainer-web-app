"use client";
import type React from "react"
export default function ClientWorkout2Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return <div className="min-h-screen bg-gray-100 font-['Sen']">{children}</div>
}
